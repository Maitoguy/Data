{
  "name": "tailnode-technologies-backend",
  "version": "1.0.0",
  "description": "Fetch from API and add it to database",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch backend --exec babel-node backend/sever.js",
     "build": "rm -rf dist && babel backend -d dist && cd frontend && npm install && npm run build",
    "serve": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Akash Bhande",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "mongoose": "^7.5.3",
    "nodemon": "^3.0.1"
  }
}
